# sensei-quiz（オリジナル先生）

早稲アカ小3向けのテスト対策クイズアプリ。子供が実際に使う。

## 構成

ビルドツールなしの**静的HTML＋Vercelサーバーレス関数**。
各HTMLは1ファイル完結（CSSもJSも内包）で、共有ライブラリはない。

```
index.html        ホーム。各アプリへの導線 ＋ 全アプリ共通のカードコレクション画面
chizu.html        ニッポン制覇（都道府県マップ→クイズ→メダル→守護神カード）
                  通年で使う。テストとは独立
october.html      10月マンスリー（後期 第1〜5回）。★開発中・いま一番新しい
natsuyasumi.html  夏休み理科特訓（2026年夏）。★終了
rikashakai.html   7月マンスリー。★終了・凍結
may.html          5月マンスリー。★終了・凍結
api/generate.js   Claude API 呼び出し。教材写真→クイズ生成／漫画構成案
Cards.json        カード98枚のマスタデータ
stories.json      学園の読み物63話。10月版のご褒美（れんしゅう20問ごとに1話）
pref-stories.json 守護神ミッション10話。ニッポン制覇のご褒美（県のメダル10個で1話）
quizbank-seed.json  作りおき問題675問。理科・社会とも後期第1〜5回ぶん。
                    テキストと練成問題集の両方から作った。
                    october.html の管理画面から読みこむ（承認まちに入る）
card-images/      カード絵（WebP。q85 で変換済み）
img/figures/      問題に出す図（自作のSVG）。教材の写真は使わない
chars/            守護神の立ち絵（portraits/）とメダル画像（medals/地方名/）
img/              HTMLから外出しした画像
                    kv.jpg      理科☆社会特訓のキービジュアル
                    hero.jpg    ホームのヒーロー画像
                    teachers/   先生の立ち絵（natsuyasumi / rikashakai）
                    manga/      佐賀県の読み物 cover / p1〜p8 / summary
                    reactions/  may.html のクイズ中の先生の表情
                                <先生>_<start|ganbaro|correct|wrong>.webp
```

**画像をHTMLに base64 で埋め込まないこと。** 以前は4ファイルに計4.7MBが
埋まっていてHTMLが1.8MBになっていた。`img/` に置いてパスで参照する。

## テストごとに1ファイル、終わったら凍結

`may.html`（5月）→ `rikashakai.html`（7月）→ `natsuyasumi.html`（夏休み）
というように、**テストや講習ごとに独立したHTMLを1本作る**のがこのプロジェクトの
やり方。そのテストが終わったらそのファイルは**凍結**し、以後さわらない。
過去のテストの成績や記録がそのまま残るようにするため。

だから「古いから統合する」「共通化してリファクタする」はしない。
次は10月マンスリー用に新しいファイルを作る予定。

### 新しいテストのアプリを作るとき

そのときいちばん新しいファイル（今なら `october.html`）を土台にする。
そのうえで **保存領域を必ず見直すこと。**

`natsuyasumi.html` と `rikashakai.html` は、下記をすべて**共有**している:

- IndexedDB `senseiZukan` の `entries`（読み物）、`materials`（教材写真）、
  `quizbank`（問題ストック）
- localStorage の `sz_review_v1`（復習リスト）

つまり夏休みアプリに入れた読み物が7月アプリにもそのまま出る。単純にコピーすると
10月アプリもこの2つと中身を共有してしまう。テストごとに内容を分けたいなら、
新しいファイルでは IndexedDB のDB名かキー名、および `sz_review_v1` を
別の名前にする（例: `senseiZukan_oct` / `oct_review_v1`）。

一方 **カードの保存先 `sz_cards_v1` は共有のままでよい。** カードは
テストをまたいで1つのコレクションにまとまる設計で、`index.html` が
`sensei-gacha-v1` と合わせて合算表示している。新しいキーを作った場合は
`index.html` の `sources()` に追加すること。

デプロイは Vercel。`api/` 配下が自動的に関数になり、それ以外は静的配信される。
`vercel.json` はない（デフォルト動作に任せている）。

## 一番大事な前提：サーバーDBがない

**成績もカードも全部ユーザーの端末のなか**にある。ログインもアカウントもない。
つまり **端末を変えるとデータは消える**。この制約を壊さないこと。

### localStorage キー一覧

| キー | 中身 | 書く人 | 読む人 |
|---|---|---|---|
| `sensei-gacha-v1` | マンスリー系のカード＋チケット | may, chizu | index |
| `sz_cards_v1` | 理科☆社会・夏休み特訓のカード | natsuyasumi, rikashakai | index |
| `sensei-quiz-v15` | 5月テストの成績 | may | natsuyasumi |
| `sensei_collection_v2` | 都道府県メダルの進捗 | chizu | chizu |
| `sz_challenge_v1` | 50問チャレンジの連続正解記録 | natsuyasumi | natsuyasumi |
| `sz_review_v1` | まちがえた問題の復習リスト | natsuyasumi, rikashakai（**共有**） | 同左 |
| `cards_reset_v2` / `sz_cards_reset_v2` | 1回だけリセットを走らせるフラグ | index, natsuyasumi | 同左 |

`index.html` はこの**複数のキーを横断して合算**し、1つのコレクション画面として見せている。
カードの保存先を増やしたら `index.html` の `sources()` にも足すこと。忘れると
「取ったのにコレクションに出てこない」というバグになる。

### IndexedDB

DB名 `senseiZukan` / ストア `kv`。natsuyasumi と rikashakai が**同じDB・同じキーを
共用している**（片方に入れた読み物がもう片方にも出る）。新しいテストのアプリを
作るときの注意は上の「テストごとに1ファイル」を参照。
キーは `entries`（読み物）、`materials`（教材写真）、`quizbank`（問題ストック）。
写真データが入るので重い。natsuyasumi にはJSONの書き出し／読み込み機能があり、
端末を移すときはこれを使う。

## カードの仕組み

`Cards.json` が唯一のマスタ。98枚。1件はこの形:

```json
{ "id": "sr001", "char": "zanpanman", "rarity": 2, "series": "gakuen",
  "name": "ザンパンマン先生", "power": "必殺技の説明…",
  "imgUrl": "/card-images/sr01_zanpanman.webp" }
```

`series` は**入手経路のグループ**。`index.html` のコレクション画面がこれで区切って、
それぞれに「◯枚／◯枚」と進捗バーを出す。全部まとめた分母だと、カードを足すたびに
数がふえて「集まらない感」が出るため。カードを足すときは必ず付けること
（無いものは `gakuen` 扱いになる）。

| 値 | 枚数 | 中身 | 入手 |
|---|---|---|---|
| `gakuen` | 65 | オリジナル学園の先生たち | **ランダムに出るのはこれだけ**（10問ごと／ガチャ） |
| `pref` | 27 | 都道府県の守護神（`r063`〜`r089`） | ニッポン制覇でメダル10個 |
| `challenge` | 6 | `sp01`〜`sp03`（夏休み）、`sp04`〜`sp06`（10月） | 50問連続正解 |

画像は **WebP**（品質85）。元絵がPNGやJPEGで来たら必ず変換してから入れる。
PNGのまま置くと1枚2.5〜3MBになり、子供の端末で表示が重くなる。

レアリティは **1始まりの配列インデックス**で、0番は空文字のダミー:

```js
RNAME = ['','N','SR','SSR','R','伝説','神覚者','四天王','無限'];  // 1..8
RCOL  = ['','#aaa','#44ccff','#ff8800','#dd44ff','#ff2288','#ffd700','#66ffee','#ffffff'];
```

`rarity` の数字が大きい＝レア、ではない。**R(4) は SSR(3) より上**という並びなので、
ソートや比較を書くときは必ずこの配列を基準にすること。**8 の「無限」だけは例外で
いちばん上**（`sp06` 専用）。7 が「四天王」という四人組カードの名前で埋まっていて、
二人のカードをそこに入れられなかったため足した。

この配列は `index.html` `october.html` `natsuyasumi.html` がそれぞれ持っている
（共通化しない方針）。コレクション画面のループも `for(r=8;r>=1;r--)` になっていて、
**新しいレアリティを足すときは配列とループの両方**を直す必要がある。
`natsuyasumi.html` は凍結ファイルなので 7 までのまま。`rikashakai.html` は 5 まで。

**同じキャラが複数のレアリティで存在する。** `id` は `n010`／`sr010`／`ssr010` のように
番号をそろえ、`char` は同じにする（例 `risaikuru`）。名前は N と SR が同じで、
SSR だけ称号つきになる（`ザンパンマン先生` → `真・ザンパン魔皇`）。
`char` からカードを引くときは `find` ではなく**レアリティが低い順に並べて先頭**を取ること。
配列の並び順しだいで「影潜り 極秘院 隠真」のような別名が出てしまう（実際に踏んだ）。

### カードの入手経路

| アプリ | 条件 | もらえるもの |
|---|---|---|
| 夏休み理科特訓 | 10問こたえるごと | ランダム1枚 |
| 夏休み理科特訓 | 50問連続正解 | 専用の神覚者カード（`sp01`/`sp02`）。2枚そろうと四天王 `sp03` に融合 |
| ニッポン制覇 | 1県のメダル10個コンプ | その県の守護神Rカード（`PREF_CARD_MAP` 参照） |
| マンスリーテスト | 5問正解ごとにチケット1枚 | チケットでガチャ |

## 都道府県データ（chizu.html）

### メダルは教材テキストの中のものだけにする

**いちばん大事な決まり。** メダルのクイズは、早稲アカの社会テキスト
「ぐるっと日本ひとめぐり」に**実際に載っていることだけ**から作る。
一般的な中学受験の知識で埋めない。テキストにない語（例:「平城京」）は、
たとえ正しくても入れない。テキストが手元にない県は**新しく実装しない**。

**例外がひとつある。** `"31"`〜`"47"`（中国・四国・九州沖縄）の17県は
教材外のクイズで作られているが、**すでに子供が集めたメダルの進捗と
守護神カード17枚を消さないために残してある**。サーバDBが無く、消すと
二度ともどらないため。テキストの後半の回が手に入ったら作りなおす。
一度は消したが（`b0917b2`）、本番に出すときにもどした。

確かめかたは、`quizbank-seed.json` の社会の問題（`source` に教材からの
引用が入っている）と照らすこと:

```bash
python3 -c "
import io,re,json
s=io.open('chizu.html',encoding='utf-8').read()
i=s.index('const PREF_DATA'); j=s.index('const PREF_CARD_MAP')
parts=re.split(r'\n  "(\d\d)":\{ name:"([^"]+)"', s[i:j])
Q=json.load(io.open('quizbank-seed.json',encoding='utf-8'))
c=' '.join(q['q']+q['a']+q['source']+q['explain']+q['juken']+''.join(q['accept'])
           for q in Q if q['subj']=='shakai')
for k in range(1,len(parts),3):
    kw=re.findall(r'keyword:"([^"]+)"',parts[k+2])
    an=re.findall(r'choices:\["([^"]+)"',parts[k+2])
    hit=sum(1 for a,b in zip(kw,an) if a in c or b in c)
    print(parts[k],parts[k+1],hit,'/',len(kw))
"
```

### 実装ずみの県

`PREF_DATA` に県コードをキーとして持つ。**27県**。メダルは270個。

| コード | 地方 | カード | 教材 |
|---|---|---|---|
| `"20"`〜`"30"`（22をのぞく） | 近畿・中部の10府県 | `r080`〜`r089` | テキスト第1〜5回から **100/100個** |
| `"31"`〜`"47"` | 中国・四国・九州沖縄 | `r063`〜`r079` | **教材外（14/170個しか一致しない）。要作りなおし** |

のこり20道県（`"01"`〜`"19"`・`"22"`）は**教材が手元にないので実装しない**。
タップすると「じゅんびちゅう！」と出る。テキストの写真が来たら、その回のぶんだけ足す。
一度は入れたが、一般知識で書いてしまったため取りのぞいた（`247aaea` に骨組みが残っている）。

**守護神カードの取りこぼしは、起動時に直る。**
`chizu.html` の `reconcileGuardianCards()` が、**メダルが10個そろっているのに
カードが `sensei-gacha-v1` に入っていない県**を見つけて入れなおす。
もともと `awardGuardianCard()` が失敗しても `guardianGot` を立てていたため、
一度取りそこねると二度と取りにいけなかった（実際に踏んだ）。いまは

- `awardGuardianCard()` は `'new'` / `'have'` / `null`（失敗）を返す
- `guardianGot` は**カードが入ったときだけ**立てる。失敗したら次の正解でやりなおせる
- `sensei-gacha-v1` が無い・`collection` キーが無い・中身が壊れている場合も
  作りなおして入れる（古い保存だと `db.collection[id]` で落ちていた）

**県を外したら、`sensei_collection_v2` の進捗も自動で掃除される。**
`chizu.html` の `pruneProgress()` が、`PREF_DATA` に無い県コードの進捗を
起動時に消す。もどってくるときはクイズごと入れかわるので、古い進捗を残すと
「答えていないメダルが集まっている」状態になってしまうため。

**10府県のメダル100個の絵はそろっている**（`chars/medals/kinki/` と `chubu/`、
WebP q85・252px・1枚17KBほど・合計1.7MB）。17県のぶんは以前からのPNG。
**守護神カードの絵10枚（`r080`〜`r089`）と立ち絵10枚もそろった。**
27県ぶん全部の絵がある状態。

### 守護神ミッション（pref-stories.json）

**県のメダル10個がそろうと、守護神カードといっしょに読み物が1話開く。**
守護神と学園の先生が組んで、その県の知識で問題を解決する話。1話600〜750字。

```json
"25": { "code":"25", "no":6, "pref":"滋賀県", "guardian":"びわ湖ヌル左衛門",
        "teacher":"gokuhiininma", "teacherName":"極秘院 隠真",
        "title":"ミッション：水のゆくえを追え", "body":["段落1","段落2"] }
```

**これは `chizu.html` に置くこと。`october.html` の `stories.json` に入れてはいけない。**
october はテストが終われば凍結するが、守護神は `chizu.html`（通年）で手に入る。
凍結ファイルに入れると1か月で読めなくなる。

- **保存キーは作っていない。** 開いたかどうかは `sensei_collection_v2` の
  メダル数から分かる（10個そろっていれば読める）。増やすものが無いので壊れない
- `pref-stories.json` は起動時に `fetch` で読む。**読めなくても画面は壊れない**
  （ボタンが出ないだけ）。届くのが県の画面より遅かった場合は、届いた時点で
  `renderPrefDetail()` をもう一度呼んでボタンを出す
- **地図の画面に「📖 守護神ミッションが◯話 読めるよ」と出す。** メダルの数から
  数えるだけで、**読んだかどうかは追わない**（追うと保存キーが1つ増える）。
  これが無いと、**前からメダルをそろえてある県の話に気づけない**
  （地図は金色になるだけで、話が増えたとは出ないため）
- `.map-story-note` は CSS で `display:none` にしてあるので、出すときは
  `style.display='block'`。`''` では消えたままになる（実際に踏んだ）
- **先生は1県ごとに変える。** 10話とも別の先生で、その県の内容に合う人を当てている
  （滋賀＝隠真で「気づかれずに水を追う」、三重＝亀吉で「三日見る」＝真珠の養殖、
  兵庫＝英愛で「時計が合わない」＝日本標準時子午線、など）

**話に書いてよいのは、その県のメダル10個に出てくることだけ。**
メダルは教材テキストから作ってあるので、これを守れば話も教材の中にとどまる。
実際、初稿では3か所ずれていた:

- 安土城を「七階建て」と書いたが**教材にない**（教材は「織田信長が築き、今は石垣のみ」）
- 姫路城を「四百年ずれずに立っている」と書いたが**教材にない**（教材は「世界文化遺産」）
- 金閣と銀閣を「はなやかな時代／静かな時代」と対比したが**教材にない**
  （教材にあるのは「3代義満の別荘」「8代義政の別荘」だけ）。**時代の空気は書かない**

照合はこれで通す（メダルのキーワードと `quizbank-seed.json` の両方を見る）:

```bash
python3 -c "
import json,io,re
d=json.load(io.open('pref-stories.json',encoding='utf-8'))
Q=json.load(io.open('quizbank-seed.json',encoding='utf-8'))
s=io.open('chizu.html',encoding='utf-8').read()
mb=s[s.index('const PREF_DATA'):s.index('const PREF_CARD_MAP')]
for code,v in d.items():
    i=mb.index('\"%s\":{ name:'%code); j=mb.find('\n  \"',i+5)
    words=re.findall(r'keyword:\"([^\"]+)\"',mb[i:j if j>0 else len(mb)])
    t=''.join(v['body']); hit=[w for w in words if w in t]
    print('%-9s %d/%d %4d字  %s'%(v['pref'],len(hit),len(words),len(t),' / '.join(hit)))
"
```

いまは近畿・中部の10府県ぶんだけ。**17県（中国・四国・九州沖縄）はメダル自体が
教材外**なので、テキストが来てメダルを作りなおしてから書く。

### 守護神の立ち絵はカードから切り出す

`chars/portraits/<県名ローマ字>.webp` は**カードの絵の上半分を切り抜いて**作る。
別に絵を用意しない。カードは左にアイコンの列、上に番号と県名、まん中に名前の
プレートがあるので、その内側だけを取る:

- 横 … 画像の幅の **14.5%〜97.7%**（左のアイコン列と外枠を落とす）
- 縦 … 上から **8.5%**（番号と県名の行の下）から、**名前プレートのすぐ上**まで

プレートの位置は絵ごとにちがうので下端だけは1枚ずつ決める（0.48〜0.57 くらい）。
出来上がりは横900px・WebP q85・1枚150〜190KB。`chizu.html` の `.pd-avatar` は
240pxの丸に `background-size:cover` ＋ `background-position:top center` なので、
**切り抜きの縦はほぼ全部、横は9割ほどが見える**。顔が中央からずれていても平気。

無いあいだは立ち絵が絵文字に、カード絵は `cardImgFB` で落ちるので画面は壊れない。
守護神の名前は絵にあわせてつけた（`Cards.json` の `name` と `chizu.html` の
`guardianName` の**両方**を直すこと）。

メダルの絵は、1枚のシートに10個ずつ並んだ画像から機械で切り出している。
金色のリングを検出して中心を追いこみ、直径は20個の中央値でそろえる方式。
`chizu.html` 側の `.medal` が `border-radius:50%` ＋ `overflow:hidden` なので、
**リストの外側で切り出してよい**（アプリ側の金ぶちと二重になってちょうどよい）。

**新しく足す絵はすべて `.webp`。** `PREF_DATA` のパスも `.webp` に統一してある。

1県 = メダル10個、メダル1個 = 4択クイズ1問。県を足すときは:

1. `PREF_DATA` に県を追加（守護神＋メダル10個＋クイズ10問）
2. `chars/portraits/<県名ローマ字>.webp` に守護神の立ち絵（カードから切り出す。上記参照）
3. `chars/medals/<地方名>/<キーワード>.webp` にメダル画像10枚
4. `Cards.json` に守護神Rカードを追加し、`PREF_CARD_MAP` で県コードと紐づけ
5. SVGマップ側の `.prefecture` に対応する `data-code` があるか確認

クイズの `answer` は選択肢配列のインデックス。データ上はすべて `answer:0`（正解が先頭）で、
表示時に `shuffleArray` でシャッフルされる。**この規約を崩さないこと。**

## api/generate.js

Claude API を生fetchで叩く（SDKは入れていない）。モードは3つ:

- `yomimono` … 漫画のコマ画像 → コマごとの解説＋4択クイズ
- `compose` … テキストと問題集の写真 → 漫画の構成案＋画像生成プロンプト
- legacy … 教材写真 → 4択クイズだけ
- `flashcard` … 教材写真 → **記述式**の問題（10月版・関門1）。source（教材からの引用）必須
- `verify` … 問題＋根拠 → 一意に答えられるかの判定（10月版・関門3）。5問ずつ呼ぶ

`JUKEN_POLICY` という共通の方針文字列を全プロンプトに差し込んでいる。
中学受験を見すえた内容にするための指示なので、プロンプトを触るときは消さないこと。

モデルは `claude-sonnet-5`。`thinking: {type:'disabled'}` を明示している
（sonnet-5 は省略すると思考がONになり、思考トークンが `max_tokens` を食って
JSONが途中で切れるため）。品質を上げたくなったら `{type:'adaptive'}` にして
`max_tokens` を増やす。

**写真は送る前に縮める（`october.html` の `shrinkPhoto()`）。** 長辺1568pxをこえる
ものだけ canvas で縮め、JPEG（品質0.85）にする。スマホの写真は1枚2〜4MBあり、
数枚えらぶと **Vercel の関数が受けとれる本文の大きさ（4.5MB）をこえて失敗する**。
APIに渡る絵もこのあたりで頭打ちになるので、縮めても問題の出来は変わらない。

- 長辺が1568px以下の写真は**そのまま**送る（小さい絵を作りなおして荒くしない）
- 縮めた結果がもとより大きければ、もとのほうを使う
- HEICなど `<img>` で読めない形式だったときは、もとのまま送る（`onerror` で復帰）
- サーバ側（`api/generate.js` の `detectMedia`）は先頭バイトで形式を見ているので、
  JPEGに変換しても `media_type` は正しく付く
- サムネの下に「◯枚・送るのは約◯MB」と出るので、大きすぎるときは親が気づける
- 教材のページを写した4032×3024の写真で、**1.08MB → 0.35MB** になった

`ANTHROPIC_API_KEY` は Vercel の環境変数。レスポンスは末尾が切れることがあるので
`parseJSON()` が閉じカッコを補って復旧を試みる作りになっている。

## 書くときの約束

- **フレームワークを持ち込まない。** React も npm パッケージも入れない。素のJS。
- **ファイルをまたぐ共通化をしない。** 各HTMLは単体で開いても動く状態を保つ。
  重複はあるが、それを承知でこの構成にしている。
- **UIの文言はぜんぶ日本語。** ただし**ふりがなは付けない。** 使う子は小3だが
  『Dr.STONE』『NARUTO』を読む読解力があり、ふりがなはむしろ読みにくいとのこと。
  語彙や内容も、小3向けに薄めず、歯ごたえのあるものにしてよい。
- **既存のセーブデータを壊さない。** localStorage のキー名を変えるときは
  移行コードを書くか、`*_reset_*` フラグ方式で1回だけ変換する。
- 画像は必ず `onerror` フォールバックを付ける（絵文字などに落とす）。
  素材が欠けても画面が壊れないようにするため。

## october.html（10月版）

問題は**フラッシュカード一本**。4択は作らない。`choices` フィールドを持たないので
「次のうち正しいものは」型の問題は構造的に作れない。1問はこの形:

```json
{ "id":"q...", "type":"text|map_reverse", "subj":"rika|shakai", "round":1,
  "q":"問題文", "a":"正解（20字以内）", "accept":["表記ゆれ"],
  "source":"教材からの引用（必須・品質保証の要）",
  "explain":"解説", "juken":"入試ポイント",
  "checks":{"auto":"pass","ai":"pass","human":"approved"} }
```

問題が出題プールに入るまでの**4つの関門**。全部通ったものだけが子供の前に出る。

| | どこ | 何をする |
|---|---|---|
| 関門1 | `api/generate.js` の `flashcard` | 生成時にプロンプトで縛る。引用できない問題は作らせない |
| 関門2 | `october.html` の `gate2()` | 禁止語・答えの長さ・答えが問題文にある・引用なし・重複を機械で弾く |
| 関門3 | `api/generate.js` の `verify` | 作った時とは別プロンプトで、白紙から一意性を判定。5問ずつ |
| 関門4 | `october.html` の承認UI | 1問ずつ ⭕採用／✏️直す／🗑️捨てる。採用したものだけ `quizbank` へ |

保存領域（夏休み版とは分ける。カードだけ共有）:

| キー | 中身 |
|---|---|
| IndexedDB `senseiOct` / `kv` | `quizbank`（採用ずみ）、`drafts`（確認まち） |
| `oct_challenge_v1` | 50問連続の記録と自己ベスト |
| `oct_stories_v1` | 解放ずみの小説のid |
| `oct_review_v1` | まちがい直しリスト |
| `oct_practice_v1` | れんしゅうで解いた累計（読み物の解放に使う） |
| `oct_round_v1` | れんしゅうでえらんだ単元（科目ごと。0＝ぜんぶ） |
| `oct_retire_v1` | 作りかえた古い問題を1回だけ消したしるし（下記） |
| `sz_cards_v1` | **カードは夏休み版と共有**（`index.html` の変更不要） |

- 50問連続チャレンジの専用カードは `sp04`（理科）/ `sp05`（社会）/ `sp06`（融合）。
  **これが最終目標**として画面に出る（未クリアの科目に「🎴 50問クリアでレアカード」）。
  絵は3枚ともそろっている（`Cards.json` から消えていてもトーストだけ出して落ちない）。

  | id | 条件 | カード | レアリティ |
  |---|---|---|---|
  | `sp04` | 理科 50問連続 | 三つの知、無限の未来（花火・善記・方無） | 6 神覚者 |
  | `sp05` | 社会 50問連続 | 四先生 知は、世界をつなぐ | 6 神覚者 |
  | `sp06` | **両方クリア**で融合 | 神覚者の約束（教王と学王の子ども時代） | **8 無限** |

  `sp06` は `tryFusion()` が両科目の `cleared` を見て1回だけ出す。話としては
  `sp01`／`sp02`／`sp03`（四天王 鍵の継承者たち）の前日談にあたる。
- **ランダムに出していいのは `gakuen` シリーズだけ。** 抽選プールは
  `series==='gakuen'` で絞ること（除外リストではなく、許可リストで書く）。
  - `challenge`（`sp01`〜`sp06`）は**50問連続クリア**のごほうび
  - `pref`（`r063`〜`r089`）は**ニッポン制覇でメダル10個**のごほうび

  どちらもランダムで出ると、その挑戦をする意味がなくなる。
  `october.html` の `drawOne()`、凍結ずみの `may.html` と `natsuyasumi.html` の
  抽選プール、いずれも `gakuen` だけにしてある。
- **`pickCard()` は2回引いて、片方だけ未取得ならそちらを採用する。**
  完全ランダムのままだと、集まってきた子ほど「所持カード」ばかり出る。
  `natsuyasumi.html` と `may.html` は**未取得だけを先に配る**作りだが、それだと
  未取得が先に出きって、そのあと新しいカードが二度と出なくなる。
  軽い下駄にとどめて、新しいカードがテスト期間じゅう散らばるようにしてある。
  レアリティの配分（N52/SR23/SSR15/R8/伝説2）は `drawOne()` のままで変わらない。
  - 目安（学園54枚のうち11枚が未取得のとき）: 920問＝カード92枚で
    新カード **9.6枚・70問に1回**。完全ランダムだと 8.0枚・91問に1回だった。
  - 新カードをもっと出したいなら、枚数を足すのがいちばん効く。
    R（レアリティ4）は3枚しかなく8%の枠を分けていて歪んでいたが、
    `r036`〜`r039` を足して**7枚**にした（1枚 2.67% → 1.14%。SSRとほぼ同じ）。
  - **学園カードの id は接頭辞ごとに別の番号帯**。`n0xx`/`sr0xx`/`ssr0xx` は
    同じ番号＝同じキャラだが、`r0xx`（R）と `l0xx`（伝説）はそれとは無関係の
    独立した番号列になっている（例: `n001`=ザンパンマン、`l001`=ノミソラーメン）。
    R を足すときは `r030`〜`r032` のつづきではなく、**どの接頭辞にも使われていない
    番号**を選ぶと混乱しない（`r036`〜`r039` はそうやって決めた）。
  以前は除外していなかったため、レアリティ4（R）を引くと**9割の確率で守護神**が
  出ていた（R枠30枚のうち27枚が `pref` だったため）。除外リスト方式だと、
  カードを足すたびに同じ穴が空く。
- **結果画面に「❌ まちがえた◯問だけ」ボタンがある。** その回でまちがえた問題だけを
  その場でもう一度やる。`qWrong` に `score()` がためて、`beginQuiz()` で空にする。
  - 全問正解のときと、50問連続チャレンジのときは出さない
    （チャレンジは1問ミスでその場終了なので、やりなおす意味がない）
  - `startWrongOnly()` は `beginQuiz()` が `qWrong` を消す前に控えをとること
  - モードは `'free'` なので、読み物のカウント（`practiceCount`）に入る
  - これとは別に、ホームの「まちがい直し」は**累積**のリスト（`oct_review_v1`）。
    3回連続で正解するとリストから消える（`MASTER=3`）。
    `startReview()` が `review.slice(0,20)` と**古いほうから20問**取るので、
    リストが20問をこえていると、さっきまちがえた問題が出てこない。
- **れんしゅうは単元（第◯回）でしぼれる。** ホームの科目カードに
  「ぜんぶ／第1回…第5回」のチップが出て、それぞれ問題数も見える。
  えらんだ回は `oct_round_v1` に科目ごとに残る（0＝ぜんぶ）。
  - 出す回は `roundOf(subj)`。**えらんだ回の問題が0問になっていたら
    自動で「ぜんぶ」にもどる**（問題を捨てたり入れなおしたりしたときのため）。
  - 回の一覧は `roundsOf(subj)` が `quizbank` から作る。第6回が来ても勝手に増える。
  - **50問連続チャレンジは、いつも全部の回から出す。** 1回ぶんだと50問に足りず
    同じ問題がくり返し出てしまうため。単元しぼりは「れんしゅう」専用。
- **読み物は「れんしゅう」のごほうび、レアカードは「50問連続チャレンジ」のごほうび。**
  役わりをきっちり分けてある。
  - れんしゅう（`mode==='free'`）とまちがい直し（`'review'`）で
    **`STORY_EVERY`（20問）こたえるごとに1話**。れんしゅうが1回20問なので
    **1回＝1話**にそろえてある。数えているのは `practiceCount`
    （`oct_practice_v1`）で、**チャレンジの解答は数えない**。
  - **50問連続チャレンジでは読み物は開かない。** あちらは `sp04`/`sp05`/`sp06` を取りにいく場所。
  - 最終話だけは例外で、融合カード `sp06` を取ったときに開く（下記）。
  - 目安: **62回（1240問）で最終話をのぞく62話**。問題バンク675問の2周ぶん。
    1日20問なら1か月半もつ。
  - 一度 10問ごと（1回2話）にしたが、460問＝1日20問で3週間で在庫切れになる。
    いちばん追いこみたい時期にごほうびが薄くなるので20問にもどした。
    **話を書きたしたら、この数を減らしてよい。**
  - 以前はチャレンジの節目（5・10…45）とクリアで開く作りだった。だが50問連続は
    1問ミスでその場終了で（自己採点の正答率95%でも1回の成功率は8%ほど）、
    ベストが50に達したあとは読み物が進まなくなる。**子供が実際にやるのは
    れんしゅうのほう**なので、そちらに move した。
  - 以前は1問正解ごとに1話ひらいていた。初回の挑戦だけで在庫を使いきるうえ、
    ベストが50に達したあとは二度とひらかなかった（実際に踏んだ）。
  - `unlockNext()` は在庫切れでも落ちない。切れたらトーストだけ出る。
- `startChallenge()` は**挑戦のたびに streak を0にもどす**。クリア後は streak が
  50のまま残るので、これをしないと出題が0問になって落ちる（実際に踏んだ）。
- チャレンジ専用のレアカードは**初クリアのときだけ**。2回目からは読み物が出る。
- 画面は**iPad横長**が基準（左に先生、右に問題）。解説文は `1.14rem` / 行間 `2.05`。
- `source`（引用）は**100字以内の該当箇所だけ**にする。長い文章まるごとだと画面を圧迫する。
- `gate2()` の「上の・下の・右の・左の」は、**そのあとに図や表が続くときだけ**弾く。
  そうしないと「地球上の水」のような正しい問題文まで巻きこむ（実際に踏んだ）。

### 手書きの書き込みは信じない

教材の写真は、子供が答えを書き込んだあとの問題集であることが多い。
**その書き込みは答え合わせをしていないので、まちがっていることがある。**
（実際、第1回の海水魚と淡水魚の仕分けは逆に書かれていた。）

- 手書きの文字を正解の根拠にしない。`source` に手書きを写さない。
- **印刷された文から答えが確定できる問題だけ**を作る。
- 選択肢から絞りこむ場合は、あてはまるものが1つしかないと言いきれるときだけ。
- この方針は `api/generate.js` の `flashcard` と `verify` のプロンプトにも書いてある。

問題には `basis` フィールドを持たせている:

| 値 | 意味 |
|---|---|
| `text` | 印刷された地の文から直接作った。信頼度が高い |
| `choice` | 選択肢を手がかりに絞りこんだ。承認画面で**オレンジのバッジ**が出る |

`choice` の問題は、承認するときに人が中身を確かめること。

### 図つきの問題

`type:"figure"` ＋ `fig:"flame.svg"` で、`img/figures/` の図を問題文の上に出す。
`gate2()` は figure のとき「図」「写真」などの禁止語を適用しない（図が実際に
画面に出るため）。ただし `fig` が無い figure は弾く。

**答えの写真を、問題の側に出してもよい。** `fig` はファイル名をそのまま
`/img/figures/` の下につなぐので、`fig:"ans/sekiranun.webp"` と書けば
`ansFig` 用の写真をそのまま問題側に出せる。コピーは要らない。
マンスリーテストの理科は**写真を見て見分ける問題が中心**（前回は5大問中3問）なので、
この形は実戦に近い。いまは雲の写真9枚で「別名→正しい名前」を問う9問
（`seed_cld_01`〜`seed_cld_09`）に使っている。

- **CC BY / CC BY-SA の写真を問題側に出すときは `figBy` を書くこと。** 絵の下に
  小さく出る（`.fig-by`）。答えの側の `ansFigBy` とは別のフィールドで、
  **同じ写真でも両方に書く必要がある**（出る場所がちがうため）。
  書きわすれるとライセンス違反になる
- 絵が読みこめないときは、わくごと消える（クレジットだけ残らないように
  `onerror` で `parentNode` を消している）
- `figBy` は `patchFigures()` が追随する（`ansFigBy` と同じ。あとから直せるし、
  種から消えれば消える）

**写真の問題は、問題文の末尾に〔…〕で覚え書きを書く。** 画面には出ない
（`renderQ()` が末尾の〔…〕を落とす）。白地図の問題と同じやり方で、
承認画面と重複チェックでだけ使う。写真の問題は問題文が
「写真の動物の名前は何ですか。」と全部同じになり、そのままだと
`gate2()` の重複チェックに引っかかるうえ、承認するときにどの写真か分からない。

```json
"q":"写真の動物の名前は何ですか。〔ネコ目・世界一速く走る〕"
```

- **答えを〔…〕の中に書かない。** 全文が「答えが問題文の中にある」の
  判定にかかるため
- 画面に出るのは「写真の動物の名前は何ですか。」だけ。**どの写真かを
  決めるのは絵のほう**なので、問題文で絞りこむ必要はない
- いまある写真の問題: 雲9問（`seed_cld_01`〜09。別名→正しい名前）、
  動物13問（`seed_ani_01`〜13。写真→名前）、
  理科の見分け19問（`seed_mie_01`〜20。雲9・魚5・昆虫1・鳥1・植物1・地形2）、
  **社会の見分け59問**（`seed_sm_01`〜59。建物11・古墳2・場所3・地形2・橋1・家1・
  行事4・もの9・鳥6・木6・花5・そのほかの生き物2・人物7）

**社会の見分け問題は、答えの写真97枚から59枚をえらんだ。** 残り38枚を外した理由:

| 外したもの | 理由 |
|---|---|
| 琵琶湖以外の風景（潮岬・南紀白浜・天神崎・藤前干潟・野辺山原・ポートアイランド・飛騨山脈・濃尾平野・五箇山） | 写真だけでは「どこか」が決まらない |
| 足利義満と義政、空海と最澄、蘇我馬子、藤原頼通 | 僧形・公家の肖像が似ていて、絵だけでは見分けられない |
| 彦根城・安土城跡・竹田城跡・延暦寺・善光寺・金剛峯寺 | ほかの城や寺と区別がつきにくい |
| 美濃焼と萬古焼 | 並べても見分けられない（どちらも焼き物） |
| カキツバタとハナショウブ | すじの色でしか見分けられず、小3には酷 |
| キタヤマスギ・神宮スギ・クスノキ・ウバメガシ | ただの木に見える |
| 電照菊（昼の写真）・刃物・まゆのまち・千成びょうたん（府旗）・シガ（県旗）・サクラソウとウメ（2枚合成） | 答えが絵から決まらない、または答えが一般名詞 |

**写真が「その種のふつうの姿」であることのほかに、「ほかとまぎれないか」も見ること。**
1枚だけ見て分かっても、似たものが同じバンクに入っていれば見分け問題にならない。

**見分け問題に使う写真は、その種の「ふつうの姿」であること。** ヒメマスは
`ansFig` には入れてあるが、写真が**産卵期の赤い個体**なので見分け問題からは
外した。種としては正しくても、子供は「サケ」と答えてしまう。
答えの絵（名前がわかったあとに見る）と、見分け問題（名前をあてる）では
求められる正しさがちがう。

**教材の写真は使わないこと。** リポジトリもサイトも公開されているので、
教材の写真を切り出して置くと複製と公衆送信にあたる。図は自分で描く。
SVGなら著作権の問題がなく、ラベルも日本語で自由に置ける。

いまある図: `flame.svg`（ろうそくの炎3層）、`waterscape.svg`（地面に見える水の断面）、
`cloudheight.svg`（雲の高さ3層。①②③をあてる問題用）、
`cloudtypes.svg`（十種雲形。名前・別名・高さ・つぶ・名前の読みかたを1枚に）、
`wajyu.svg`（輪中。上から見た堤防のリングと、横から見た断面。水屋と小舟つき）、
`fujigoko_map.svg`（富士五湖。西から本栖・精進・西・河口・山中の並びと、
山中湖＝水面がいちばん高い／河口湖＝いちばん低いの2つの目じるし。理科第1回の4問に付けてある）。

**「しくみ」の説明は `ansFig` ではなく `figAfter` にする。** `ansFig` は高さ250px
（せまい画面では190px）で出るので、**ラベルのある図を入れても字が読めない**
（輪中の地図を入れようとして実際にそうなった）。`figAfter` は幅いっぱい・
高さ360pxまで使えるので、ラベル付きの図はこちらに置く。
`ansFig` は「実物が1つ写っているもの」専用。

### 答えあわせのあとに出す図（`figAfter`）

問題文の上に出す `fig`（`type:"figure"` とセット）とは**別もの**。
`figAfter:"cloudtypes.svg"` を書くと、⭕❌を押す前の「かいせつ」の下に
「おぼえかた」として出る。**もう答えを見たあとなので、答えが書いてある図でよい。**
どんな `type` の問題にも付けられる（`text` でも `figure` でも）。

十種雲形は名前だけで覚えるのが苦しいので、理科・第2回の雲の問題**38問**に
`cloudtypes.svg` を付けてある。1問こたえるたびに全体の表が出る作り。

**図の中身は、必ず `source`（教材からの引用）と1つずつ照合すること。**
自作するぶん、一般知識が混ざって教材とずれやすい。実際 `cloudtypes.svg` は
初稿で5か所まちがえた:

- 「下層雲＝水のつぶ」と書いたが**教材に根拠がなく**、しかも積乱雲は
  「水や氷のつぶ」と明記されている（下層雲なのに）。つぶの記述は上層と中層だけ
- 別名のもれ: 巻積雲の**さば雲**、乱層雲の**雪雲**、積乱雲の**雷雲**、層積雲の**くもり雲**

照合はこれで通す（28項目・教材に無い語が混ざっていないかも見る）:

```bash
python3 -c "
import io,json,re
Q=json.load(io.open('quizbank-seed.json',encoding='utf-8'))
src=' '.join(q['source'] for q in Q if q['subj']=='rika' and q['round']==2)
svg=io.open('img/figures/cloudtypes.svg',encoding='utf-8').read()
names=set(re.findall(r'[巻高層積乱]{1,2}雲|かなとこ雲|入道雲|雷雲|すじ雲|うろこ雲|いわし雲|さば雲|うす雲|ひつじ雲|おぼろ雲|雨雲|雪雲|わた雲|うね雲|くもり雲|きり雲', svg))
print('教材に出てこない語:', [n for n in sorted(names) if n not in src] or 'なし')
"
```

**`figAfter` はあとから足せる。** `loadSeed()` の `patchFigures()` が、
すでに `quizbank` に入っている問題に `figAfter` だけを入れる。
一度読みこんだ問題は `q` の文で重複とみなされて足しなおせないので、これが要る。
`id` でつきあわせ（無ければ `q` の文）、**すでに図が入っているものはさわらない**ので、
承認画面で人が直した解説や引用は上書きされない。
使いかたは、管理画面の「作りおきの問題を読みこむ」をもう一度押すだけ。

### 答えの「実物」の絵（`ansFig`）

答えが実物のあるもの（鳥・木・花など）のとき、`ansFig:"shirakaba.svg"` と書くと
**答えのわくの中、大きな文字の上**にその絵が出る。置き場は `img/figures/ans/`。
`fig`（問題文の上）や `figAfter`（かいせつの下）とは出る場所がちがう。

- **「こたえ」というラベルは置いていない。** 金のわくと金の文字でそれとわかるので、
  ラベルがあると、いちばん目立つのが小さな「こたえ」の字になってしまう。
  答えの字は `clamp(2.2rem,5.6vw,3.5rem)`。7字をこえたら `len-m`、10字をこえたら
  `len-l` と、`reveal()` が文字数でクラスを切りかえて落とす（7字以上は全体の12%）。
  一度 4.6rem まで上げたが、大きすぎたので下げた。**絵と答えが主役で、
  画面を占領しない**くらいがちょうどよい。
- **拡張子は何でもよい。** `ansFig` はファイル名をそのまま使うので、`.svg` を
  `.webp` に差しかえれば実写になる。イラストと写真は混在してよい。
- **絵は起動のたびに自動で届く。** `syncFigures()` が、アプリを開くたびに
  `quizbank-seed.json` を読んで `patchFigures()` を走らせる。問題そのものは
  足さない（承認の流れを飛ばさないため）。図とクレジットだけを取りこむ。
  - ホームを出してから走らせる（`await` しない）ので、起動は遅くならない。
    つながらなければ何もしないだけで、これまでどおり動く
  - **これが無いと、あとから絵を足しても子供の画面に出てこない。** 管理画面の
    「作りおきの問題を読みこむ」を押すまで届かず、実際に「出てこない」と
    言われて気づいた。押し忘れる前提で作ること
  - 管理画面のボタンからも同じ `patchFigures()` が走る（そちらは問題も足す）
- 読みこみに失敗したら `onerror` で消える。絵が無くても画面は壊れない。

いまある26問ぶん（近畿・中部10府県の**県の木・鳥・花**と、理科第3回のメジロ）。
すべて自作SVG・200×200・まるい背景の中に描いてある。

**実写を使うときの決まり。** 教材の写真はぜったいに使わない（公開リポジトリ
なので複製・公衆送信になる）。実写を入れるなら次のどれかにする:

| 出どころ | 条件 |
|---|---|
| 自分で撮った写真 | 制限なし。いちばんよい |
| パブリックドメイン／CC0 | クレジット不要 |
| CC BY / CC BY-SA | **撮影者名とライセンス名の表記が画面に要る** |

**県章の絵は守護神カードから切り出した。** 守護神カードを作るときに県の
シンボルマークを渡してあるので、`card-images/r80`〜`r89` の**右上に県章が
描きこまれている**。色のかたまり（連結成分）だけを取り出して白地に置き、
`img/figures/ans/<県名>kensho.webp` にした（長野・岐阜・愛知・三重・奈良・和歌山の6枚）。
大阪と滋賀は Commons の PD の旗が手に入っていて、**同じやり方でカードから切り出したものと
形が一致した**ので、のこりも使えると判断している。ただし**カードの絵はAIが描いたもの**
なので、本物とちがうものが見つかったら同じファイル名で差しかえること
（`patchFigures()` が `ansFig` を上書きするので、すでに問題が入った端末にも届く）。

**写真は切り抜かない。縮めて変換するだけ。**

```bash
python3 tools/photo2ans.py <写真> <名前> [x0 y0 x1 y1] [--nobg]
```

長辺720px・WebP q85（1枚30KBほど）。**形はCSSがやる。** `.ans-fig` は
**高さだけを決めて、横は絵にまかせている**ので、写真は縦横どんな比でも
そのまま入れられる。角は `border-radius:14px` でまるめる。自作SVGは正方形
なので、まるく見える。

高さは **iPadなど広い画面で250px、840px以下では190px**。はじめは170pxだったが、
iPadで見ると小さかったので上げた。答えのわくは幅735pxほどあるので、
250pxにしても横幅ははみ出さない（`max-width:100%` で止まる）。

以前は画像を円形に切り抜いていたが、**1枚ごとに切り抜く範囲を決める手間が
かかるうえ、全身が入らなかった**（ハダカデバネズミはしっぽが切れていた）。
写真は120枚ほど入れる予定なので、手のかからないほうにした。

**切り抜く範囲は省略してよい。** 指定するのは、**被写体が小さくて余白ばかりの
とき**だけ（ラーテルは草ばかりだったので寄せた）。まず範囲なしで作ってみて、
小さすぎたら足す、の順で判断する。

**白地に切りぬかれた絵（復元図など）は `--nobg` で白を抜く。** アプリの背景が
暗いので、白いままだと四角い白い箱に見える。白からの距離でアルファを決めるので
毛の縁は硬くならないが、**白っぽい部分が被写体にあるときは抜けていないか
必ず確かめること**（スミロドンは牙が白いので心配したが、少し灰色がかっていて残った）。

**出どころは `img/figures/ans/CREDITS.md` に必ず記録する。**

**CC BY / CC BY-SA の写真は、`ansFigBy` にクレジットを書く。** 絵のすぐ下に
小さく出る（`.ans-by`）。PD / CC0 なら書かなくてよい。

```json
"ansFig":"senzanko.webp",
"ansFigBy":"写真: Ajit Huilgol / U.S. Fish and Wildlife Service（CC BY 2.0）"
```

- **絵が読みこめなかったら、クレジットも一緒に消す。** 絵が出ていないのに
  撮影者名だけ残ると意味がわからなくなるため、`onerror` で両方消している。
- `ansFigBy` は `patchFigures()` が**あとから直せる**（`ansFig` とちがい、
  すでに入っていても中身がちがえば上書きし、**種から消えていれば消す**）。
  表示が要るものなので取りこぼすとライセンス違反になるうえ、
  **絵を別の写真に差しかえたとき、古い撮影者名が残ると誤表示になる**ため。

**写真を差しかえるときは、ファイル名を変えない。** `ansFig` の値が同じなら、
すでに問題を読みこんだ端末でも絵だけが入れかわる（カルデラをブロモ山から
阿蘇山に、琵琶湖を空撮から衛星写真に差しかえたのはこの方法）。
クレジットは上のとおり `patchFigures()` が追随する。

Commons のライセンスの見わけかた（画像ページの `cc` の行を見る）:

| 表示 | やること |
|---|---|
| `Public domain` / `PD` / `CC0` | 何も要らない |
| `Creative Commons Attribution`（CC BY） | `ansFigBy` を書く |
| `Attribution-ShareAlike`（CC BY-SA） | `ansFigBy` を書く |
| `NonCommercial`（NC）や `NoDerivatives`（ND） | 使わない |
| `GFDL` **だけ** | 使わない（ライセンス全文を一緒に配る義務がある） |
| `GFDL` ＋ `CC BY-SA` の併記 | **CC BY-SA のほうで使える。** `ansFigBy` を書く |

**GFDL は古いファイルでよく出てくる。** 2005年ごろに英語版Wikipediaへ自分で
上げた写真は `{{GFDL-self}}` が付いていることがあり、2009年の移行で
CC BY-SA 3.0 が併記されたものと、GFDLのままのものがある。
**ライセンス欄を下までスクロールして、CC BY-SA の箱があるかを見ること。**

**箱が複数あったら、いちばんゆるいものを選んでよい。** 撮影者が並べて出している
ものはどれを使ってもよい。`rokkosanchi.webp`（神戸港）は GFDL / CC BY-SA 3.0 /
**CC BY 2.5** の三本立てだったので CC BY 2.5 を選んだ。CC BY なら継承（自分の
リポジトリも同じライセンスにする）の義務が付かない。

**写真がほしいものの一覧は `img/figures/ans/ほしい画像リスト.md`。** どの答えに写真が
効くかを優先度つきで並べてある（生き物33・地形19・建物26・工芸30・肖像14で、129問ぶん）。
写真が届いたらそこから消していく。

**絵は「その種」で合っているかを1枚ずつ確かめること。** `cloudtypes.svg` と同じで、
自分で描くと一般知識が混ざる。とくに取りちがえやすいもの:

- **カキツバタ**は花びらのまん中が**白いすじ**、**ハナショウブ**は**黄色いすじ**。
  ここだけで見分ける（アヤメは網目）。`kakitsubata.svg` と `hanashobu.svg` は
  そのように描き分けてある
- **ライチョウ**は冬の白い羽と、目の上の**赤いにくかん**
- **シラカバ**は白い幹の**黒い横しま**。これが無いとただの木になる
- **ハナノキ**は春、葉より先に**枝いっぱいの赤い花**。モミジと同じ絵にしない

### 白地図の問題（`type:"map_reverse"`）

日本地図の1県が光り、問題文はいつも「光っているのは、どこ？」に固定される
（`renderQ()` が `q.q` を無視して上書きする）。`a` は都道府県名そのもの。
`october.html` の `CODE_TO_PREF` にある47都道府県すべてが使える
（`chizu.html` の17県しばりとは無関係）。

- `q` は画面に出ないが、**承認画面と重複チェックで使うので1問ごとに別の文字列**にする。
  `光っている都道府県はどこですか。〔近畿・府庁所在地は大阪市〕` のように書いておくと
  承認するときにどの県かがわかる。県名そのものを入れると `gate2()` の
  「答えが問題文の中に書いてある」に引っかかるので入れない。
- 地図は縦に大きいので、`renderQ()` が `.qmain` に `map-mode` を付けて
  **左に地図・右に問題文と答え**の2カラムにしている。1カラムのままだと
  iPad横長で「⭕できた／❌まだ」が画面の下に隠れる（実際に踏んだ）。

いまある白地図問題: 京都・奈良・大阪・和歌山・兵庫・滋賀・愛知・三重・長野・岐阜の10問
（社会テキスト「ぐるっと日本ひとめぐり」で出てくる府県）。

### 読み物（stories.json）

1話はこの形。`body` は段落の配列で、1話 400〜1000字くらい。

```json
{ "id":"s11", "no":11, "title":"三十六本目のかさ", "kind":"mystery",
  "chars":["risaikuru","zanpanman","heart"], "body":["段落1","段落2"] }
```

- `kind` は `mystery` / `school` / `comedy` / `arc` の4つ。`october.html` の
  `kindName()` が日本語に直している。ここにない値を入れると「よみもの」になる。
- `chars` は **`Cards.json` の `char` の値**。カード名で表示され、持っているカードは
  色がつく。存在しない値を書いても落ちないが、キーがそのまま画面に出る。
- `no` は1から連番。`id` と順番を合わせること。**解放は配列の先頭から順番**なので、
  途中に足すと話の順序がくずれる。**新しい話は必ず最後に足す。**
- **ふりがなは付けない。** 内容も小3向けに薄めない（『Dr.STONE』を読む子が対象）。

`arc`（学園の秘密）は1本につながった話になっている。第7・8・10・13・16・18・20話で
「開かずの扉」の謎が始まって完結する。**この7話の順番は入れかえないこと。**
第22・23話は歯車善記の話。**カードの「ガオウ」は学王のこと。**
第40話「はじめての生徒」が、開いた扉のその後をしめる話。
善記は教王の息子でありながら学王の理解者、という設定で第18話とつながっている。
学王＝学ぶ人、教王＝教える人という設定は `Cards.json` の `sp01`／`sp02`／`sp03`
（四天王 鍵の継承者たち）と結びついている。

**第41〜46話は、教王と学王の子ども時代**（`sp06`「神覚者の約束」のカードと対）。
公園での出会い → 答えをめぐる最初の言いあらそい → チョークの扉 → 木の鍵と役わりの
名づけ → はじめての教室とノート → 扉を刻む約束、という順で、
**この6話もつづきものなので順番を入れかえないこと。**
第18話の『君は教える人で、私は学ぶ人だった』、第20話のノートの一ページ目
『教える』『学ぶ』、第23話の『白いページ』は、すべてここが出どころになっている。
「教王」「学王」は子どもがあそびで思いついた名前、という設定。

**第47話「あの公園」が最終話。** 第41〜46話（前日談）だけを足したときは、
最後に読むのが「はじまりの話」になって現在が閉じなくなったため、
現在にもどる話を1本足してしめた。

**最終話だけは順番では開かない。** れんしゅうを何問解いても開かない。
最終話は `stories.json` の **`"final": true`** で決まる（いまは `s47`「あの公園」）。
`october.html` の `finalStoryId()` がそれを探し、`unlockNext()` はその1話を飛ばす。
開くのは `unlockFinalStory()` だけで、条件は**融合カード `sp06` を持っていること**。
`tryFusion()` がカードを出したときと、起動時（先にカードを取っていた場合の救済）に
呼ばれる。一覧では「さいごのカードでひらく」と出る。
**最後のカードと最終話が対**、という作りにしてある。

以前は「配列のいちばん最後」を最終話としていたが、それだと**話を足すたびに
最終話が入れかわってしまい、末尾に足せなかった**。フラグ方式にしたので、
**つぎに話を足すときは、ただ末尾に足すだけでよい。**
`no` と `id` を振りなおす（途中に差しこむ）のはやめること。`oct_stories_v1` は
解放ずみの **id** を持っているので、番号がずれると、その子がすでに読んだ話と
これから読む話の対応が入れかわってしまう。

**第48話「五百年の一本」は剣道満子とザンパンマンの話。** `arc` には
「開かずの扉」とは別に、この筋もある。第21話「一本」で書いた
『一族は戦国のころ、真の名将をさがして日本じゅうを歩いた』『すきを見せるのは、
相手が本気で来ると信じているから』を受けている。設定は:

- ザンパンマンは**戦国から五百年生きている**。当時、**兵糧をあずかる係**だった
- 満子の一族は剣では負けなかったので、相手は**兵糧攻め**にした。八十日で滅びた。
  刃は一度も交わっていない
- 第36話の『わしは残飯から生まれた』は、落城後に鍋の底に残った飯を食べたこと
- 五百年 社会科を教えているのは罰。『忘れたら、なかったことになる』
- 満子が十年前にこの学園へ来たのは、ザンパンマンを斬るため。寸止めでやめて、
  かわりに朝六時の素振りに八十日通わせることにした

**第49〜52話は、子供が続きをほしがったので足した4話。** つづきものではないので、
順番の決まりはない（末尾に足しただけ）。設定はここで決めたので、
あとから書くときはこれに合わせること:

- **第49話「はじめてのワン」**（英愛未来・ワンボックス）… ワンボックスは、
  英愛が一年生のときに書いて**動かなかった三行のプログラム**から生まれた。
  だから「うまくいかなかったもののつづき」。なんでもやってくれるが、
  英愛は**自分で作れなくなること**を選ばない、という関係
- **第50話「おこった星」**（李祭久留・プロコーチロボット・ワンボックス）…
  ゴミの山の下から星のうなり声がする。**あやまるだけでは変わらない**ので、
  プロコーチロボットが「今日の一段」から練習メニューを組む。
  十日でゴミ置き場が資源の置き場になり、音は「息をしている音」に変わる
- **第51話「間に合わない理由」**（遅刻間二合・記録院筆記・ツッコミマン）…
  間二合は**学園でいちばん早く家を出ている**。通学路で次々に人に呼びとめられ、
  ぜんぶ引き受けるから遅刻する。みんなの言いぶんは「間二合くんなら走ってくれるから」
- **第52話「まだ書かれていない本」**（栞挟太郎・者郎方無・記録院筆記）…
  栞が借りるのは**貸出回数が0の本だけ**。「一度も開かれていない本は、
  まだ書かれていないのと同じ」。読み終えた本の最後のページに栞を残していく

**第53〜62話も、つづきものではない単発。** 出番の少なかったキャラを中心にした。
新しく決めた設定はこれだけ:

- **藍志位池免**（第53話）… 将棋は学園一だが、説明が長すぎて一年生が逃げる。
  プロコーチロボットの「使っていいことばは五文字」で教えられるようになった
- **兎田亀吉**（第54・62話）… 飼育委員の口ぐせは「**三日見る**」。
  一年生のときにウサギを死なせたのが、そのはじまり。小屋のかべの紙は
  『きょうの　ちがうところを　ひとつ　さがす』
- **栞挟太郎**（第55話）… 目が三行先に行くので、**音読だけは下手**。
  本を閉じて中身を語らせると、教室が静かになる
- **居眠ネムル**（第61話）… 第2話の新聞配達とつながる。朝4時から6時まで
  空を見ているので、理科の観察問題だけ満点。カガ先生に観測ノートを渡された
- **絶道クロム**（第59話）… 休んだ子の机にパンを置いている。
  自分が休みがちで、『机の上になにもないと、自分の場所がなくなったように見える』
- **宝人花火**（第58話）… 水曜の放課後、屋上で朝礼の練習をしている。
  4月の朝礼で声がふるえたのを、本人だけが覚えている
- **遅刻間二合**（第60話）… プロコーチロボットは遅刻を直せなかった。
  かわりに走る速さを上げ、朝に手つだえる件数が6件から9件になった

**第63話「太陽が六センチ」は、子供が出した設定から書いた。** 『剣道満子の斬撃は
ひとふりで太陽まで届く』『前に太陽が六センチ動いた』『バクソウ先生がそう言っていた』。
噂の出どころを者郎方無がたどる形にして、第48話（ザンパンマンが八十日、朝六時の
素振りに通った話）とつないである:

- バクソウ先生は**十年前の朝、道場の窓から見ていた**。竹刀の先が朝日にかかり、
  日が六センチずれて**見えた**。錯覚だと本人も認めるが、「思わせるだけの一本だった」
- 満子先生は「太陽を動かしたことはない。**動いてほしいと思って振ったことはある**」。
  八十日通わせた男が四十日目に寝ぼうした朝、日の出がおそければまだ六時ではない、と
- 噂は訂正されない。六センチという数字だけは、だれも変えない

### 面積・人口は「数字あて」にしない

**「京都府の面積はおよそ何km²ですか」→「約4612km²」** のような、教材のデータ欄の
数字をそのまま答えさせる問題は**作らない**。

- 人口は毎年変わる。教材の数字はその年のスナップショットで、覚えるほど古くなる
- 入試で問われるのは「どちらが広い／多い」「何番目」で、数字そのものではない
- 50問連続チャレンジは1問ミスで終わるので、いちばん転用のきかない知識で切れてしまう

かわりに**比べる問題**にする（`seed_cmp_01`〜`seed_cmp_09`）。
「近畿地方で面積がいちばん広い県は」→兵庫県、
「面積はいちばんせまいのに人口はいちばん多い府は」→大阪府、というように、
同じデータ欄から作れて、数字が多少変わっても答えが変わらないものにする。

**大阪府の面積（約1905km²）と人口（約878万人）の2問だけは数字のまま残してある。**
ほかをくらべるときの「ものさし」で、`seed_s2_002`（全国で2番目に小さい）や
`seed_s2_004`（人口3番目）とつながっているため。

**すでに端末に入った問題は、種ファイルから消しても消えない。** `quizbank` は
IndexedDB にあるので、`october.html` の `retireOld()` が id と問題文で照合して
1回だけ消す（`oct_retire_v1` を立てる）。`quizbank`・`drafts`・`oct_review_v1` の
3つとも掃除する。**今後も問題を作りかえるときは、この `RETIRED` に足すこと。**
管理画面の「削除」ボタンは先頭40問しか出ないので、手では消しきれない。

### 教材に出てくる名前は、かならず「答え」にする

理科のテキストは、生き物や植物の名前が一覧で出てくる（季節の移り変わりの
昆虫・花・鳥、魚、ほ乳類の写真の説明書きなど）。以前のバンクは内容の問題は
入っていたが、**名前が問題文の中に出てくるだけで、答えにはなっていない**ものが
多かった（十和田湖は5問に出てくるのに、「十和田湖」と答える問題は0問だった）。
読めば分かっても、自分で名前が出てくるようにはならない。

`seed_nm_01`〜`seed_nm_71` はこの穴を埋めた71問。形は3つ:

- **一覧の裏返し** … 「秋に鳴く昆虫としてあげられているのは、キリギリス・
  マツムシ・ウマオイとあと一つ何ですか」→エンマコオロギ。一覧の数だけ作る
- **季節あて** … 「エンマコオロギやマツムシが鳴くのは、どの季節ですか」→秋
- **仕分け** … 「タヌキは、食べるものによる分けかたでは何動物ですか」→雑食動物。
  テキストで肉食・草食・雑食の見出しの下に写真だけならんでいる動物に使う
  （本文に名前が無いので、名前を答えさせる手がかりが作れないため）

**新しいページが来たら、まず名前を全部書き出して、答えになっているかを見ること。**
写真の説明書き（「※放流された魚」「※外国からきた魚」など）も印刷なので根拠にしてよい。
「解いてみよう」の線つなぎの**手書きは使わない**。

### 「AとBのどちらですか」型は作らない

「食塩と砂糖のどちらが多くとけますか」のような二者択一は、答えが問題文の
中に入ってしまうため `gate2()` が弾く。これは正しい判定で、2択は
当てずっぽうでも半分当たり、フラッシュカードとして弱い。
**「より多くとかすことができるのは何ですか」**のように開いた問い方に直すこと。
図つきでも同じ。「A・B・Cのうちどれ」は作らず、「Aの部分の名前は」にする。

## 検証

テストランナーはない。変更したら最低限これを通す:

```bash
# インラインJSの構文チェック
node -e "const fs=require('fs'),vm=require('vm');
for(const f of ['index.html','chizu.html','may.html','natsuyasumi.html','rikashakai.html']){
  const s=fs.readFileSync(f,'utf8');let m,i=0;
  const re=/<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/g;
  while((m=re.exec(s))){i++;try{new vm.Script(m[1]);}catch(e){console.log('NG',f,i,e.message);}}
  console.log('ok',f,i);}"

node --check api/generate.js

# quizbank-seed.json のチェック。型（引数の位置ずれ）と gate2 相当を両方見る。
# juken にリストが入っていたら位置ずれ。accept へ移すこと。
python3 -c "
import json,os,re
Q=json.load(open('quizbank-seed.json'))
SPEC={'id':str,'type':str,'subj':str,'round':int,'q':str,'a':str,
      'accept':list,'source':str,'explain':str,'juken':str,'basis':str}
BAN=['図','写真','グラフ','表を','傍線','下線','次のうち','次の中','選びなさい','記号で答え']
POINT=re.compile('[上下右左]の\\s*(図|表|写真|グラフ|文|文章|選択肢|語群|ア|①|1|A)')
bad=0; seen={}
for q in Q:
    n=q.get('q','?')[:26]
    for k,t in SPEC.items():
        if k not in q or not isinstance(q[k],t): print('NG 型',n,k); bad+=1
    t_,a,fig=q.get('q',''),q.get('a',''),(q['type']=='figure' and q.get('fig'))
    if q['type']=='figure' and not os.path.exists('img/figures/'+q.get('fig','')):
        print('NG 図がない',n); bad+=1
    for w in BAN:
        if w in t_ and not (fig and w in ('図','写真','グラフ','表を')): print('NG 禁止語',n,w); bad+=1
    if not fig and POINT.search(t_): print('NG 画面にない図表',n); bad+=1
    if not a or len(a)>20: print('NG 答えの長さ',n); bad+=1
    elif q['type']!='map_reverse' and a in t_: print('NG 答えが問題文中',n,a); bad+=1
    if len(q.get('source',''))<10: print('NG 引用なし',n); bad+=1
    if not q.get('explain'): print('NG 解説なし',n); bad+=1
    if t_ in seen: print('NG 重複',n); bad+=1
    seen[t_]=1
print('エラー',bad,'件 /',len(Q),'問')
"

# Cards.json の画像参照が全部実在するか
python3 -c "
import json,os
for c in json.load(open('Cards.json')):
    p=(c.get('imgUrl') or '').lstrip('/')
    if not p or not os.path.exists(p): print('missing:', c['id'], c.get('imgUrl'))
"
```

## 既知の課題

- 端末間のデータ移行手段が natsuyasumi のJSON書き出ししかない。
- `img/manga/` と `img/teachers/` はJPEGのまま。WebP化すればさらに3割ほど減る。
- `chars/` の**17県ぶん**（立ち絵25MB・メダル19MB）がPNGのまま。近畿・中部の
  10府県は WebP にしてあるので、同じ手順で変換できる。ただし参照が
  `chizu.html` の `PREF_DATA` 内に散らばっているので、パスも `.webp` に直すこと。
